/**
 * agentic-scaffold — entry point.
 *
 * Replace this with your application's bootstrap logic.
 * The scaffold provides the project infrastructure; you provide the code.
 */
import { hello } from './core/hello.js';

const message = hello('agentic-scaffold');
console.log(message);
